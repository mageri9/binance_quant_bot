
```markdown
# 📋 Шаблон Задачи для Codex / AI-Developer

Этот шаблон используется для формирования промптов и задач для Codex/Claude при реализации функционала MarketMind v1.0. 

> **Правило:** Каждая задача должна строго соответствовать `STANDART.md`, `DEPENDENCY_RULES.md` и выбранному шагу из `ROADMAP.md`.

---

```markdown
## 🎯 Задача: [Название задачи, например: Реализация ReconciliationService]

### 📌 Контекст и Фаза Роадмапа
- **Фаза Роадмапа:** Фаза [1/2/3/4/5], Пункт [X.X]
- **Целевые файлы:** `src/services/reconciliation_service.py`, `tests/test_reconciliation.py`

---

### 📝 Подробное Описание
[Четкое описание того, что нужно сделать. Например: Написать сервис, который каждые 60 секунд запрашивает реальные позиции с биржи через BaseExchange, сравнивает их с открытыми позициями в таблице trades SQLite и закрывает фантомные сделки в БД при их отсутствии на бирже.]

---

### 🛑 Запреты и Правила Зависимостей (CHECKLIST)
Перед написанием кода убедись, что соблюдены следующие правила из `DEPENDENCY_RULES.md`:
- [ ] **НЕТ Redis**: Использовать только asyncio / SQLite / MemoryStorage.
- [ ] **НЕТ прямого CCXT**: Использовать только абстракцию `BaseExchange`.
- [ ] **Границы компонентов**: Модуль не содержит импортов запрещенных слоев.
- [ ] **Память (2 ГБ RAM)**: Отсутствие тяжелых циклов/утечек памяти, данные при необходимости приводятся к `float32`.

---

### ✅ Критерии Готовности (Acceptance Criteria)
1. [ ] Код написан асинхронно (`async/await`) с верными аннотациями типов Python 3.12+.
2. [ ] Добавлены / обновлены юнит-тесты в директории `tests/`.
3. [ ] Прогон `pytest tests/` завершается успешно (100% pass).
4. [ ] Код не содержит "заглушек" (`pass`, `TODO`), отработано обработкой исключений (`try/except` + loguru).

---

### 🛠 Входной Код / Контекст Смежных Модулей
<details>
<summary> Показать интерфейсы/файлы, с которыми нужно взаимодействовать </summary>

```
